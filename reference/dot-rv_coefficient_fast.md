# RV coefficient via trace trick (internal)

Multivariate generalization of R-squared. Uses O(n\*p\*q) instead of
O(n^2) by computing through cross-product matrices.

## Usage

``` r
.rv_coefficient_fast(X, Y)
```
