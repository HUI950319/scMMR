# Validate and align shared embedding with cell metadata (internal)

Validate and align shared embedding with cell metadata (internal)

## Usage

``` r
.validate_embedding(embedding, cell_meta, cell_type_col, condition_col)
```
