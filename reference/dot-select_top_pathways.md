# Select top pathway IDs by direction and significance (internal)

Select top pathway IDs by direction and significance (internal)

## Usage

``` r
.select_top_pathways(res_df, topTerm, direction, padjustCutoff, id_use = NULL)
```
