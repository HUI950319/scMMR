% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DimPlot2.R
\name{DimPlot2}
\alias{DimPlot2}
\title{Enhanced 2D Dimensional Reduction Plot}
\usage{
DimPlot2(
  seu,
  group.by,
  reduction = NULL,
  dims = c(1, 2),
  split.by = NULL,
  cells = NULL,
  show_na = FALSE,
  show_stat = NULL,
  pt.size = NULL,
  pt.alpha = 1,
  palette = "Paired",
  palcolor = NULL,
  bg_color = "grey80",
  label = FALSE,
  label.size = 4,
  label.fg = "white",
  label.bg = "black",
  label.bg.r = 0.1,
  label_insitu = FALSE,
  label_repel = FALSE,
  label_repulsion = 20,
  label_point_size = 1,
  label_point_color = "black",
  label_segment_color = "black",
  cells.highlight = NULL,
  cols.highlight = "black",
  sizes.highlight = 1,
  alpha.highlight = 1,
  stroke.highlight = 0.5,
  add_density = FALSE,
  density_color = "grey80",
  density_filled = FALSE,
  density_filled_palette = "Greys",
  density_filled_palcolor = NULL,
  add_mark = FALSE,
  mark_type = c("hull", "ellipse", "rect", "circle"),
  mark_expand = grid::unit(3, "mm"),
  mark_alpha = 0.1,
  mark_linetype = 1,
  lineages = NULL,
  lineages_trim = c(0.01, 0.99),
  lineages_span = 0.75,
  lineages_palette = "Dark2",
  lineages_palcolor = NULL,
  lineages_arrow = grid::arrow(length = grid::unit(0.1, "inches")),
  lineages_linewidth = 1,
  lineages_line_bg = "white",
  lineages_line_bg_stroke = 0.5,
  lineages_whiskers = FALSE,
  lineages_whiskers_linewidth = 0.5,
  lineages_whiskers_alpha = 0.5,
  hex = FALSE,
  hex.count = TRUE,
  hex.bins = 50,
  hex.binwidth = NULL,
  hex.linewidth = 0.5,
  raster = NULL,
  raster_method = c("scattermore", "rasterise"),
  raster.dpi = c(512, 512),
  aspect.ratio = 1,
  title = NULL,
  subtitle = NULL,
  xlab = NULL,
  ylab = NULL,
  legend.position = "right",
  legend.direction = "vertical",
  legend.title = NULL,
  theme_use = NULL,
  theme_args = list(),
  combine = TRUE,
  nrow = NULL,
  ncol = NULL,
  byrow = TRUE,
  force = FALSE,
  seed = 11
)
}
\arguments{
\item{seu}{A Seurat object or a \code{data.frame}. When a data.frame is
provided, the first two columns specified by \code{dims} are treated as
embedding coordinates, and \code{group.by} / \code{split.by} columns must
be present.}

\item{group.by}{Character. Name(s) of column(s) in metadata to group
(color) cells by.}

\item{reduction}{Character. Name of the dimensionality reduction to use.
Ignored when \code{seu} is a data.frame. Default auto-detects UMAP/tSNE.}

\item{dims}{Integer vector of length 2. Dimensions to plot.
For Seurat input: component indices (default \code{c(1, 2)}).
For data.frame input: column indices for x/y coordinates.}

\item{split.by}{Character. Column name to split into facets. Default
\code{NULL}.}

\item{cells}{Character vector of cell/row names to include. Default
\code{NULL} (all).}

\item{show_na}{Logical. Show NA group with \code{bg_color}. Default
\code{FALSE}.}

\item{show_stat}{Logical. Show cell counts in labels/subtitles. Default
\code{NULL} (auto: \code{FALSE} when theme is blank, \code{TRUE}
otherwise).}

\item{pt.size}{Point size. Default auto-calculated.}

\item{pt.alpha}{Point transparency. Default \code{1}.}

\item{palette}{Character. Color palette name (passed to
\code{palette_colors}). Default \code{"Paired"}.}

\item{palcolor}{Character vector. Custom colors. Default \code{NULL}.}

\item{bg_color}{Background color for NA points. Default \code{"grey80"}.}

\item{label}{Logical. Add group labels. Default \code{FALSE}.}

\item{label.size}{Label font size. Default \code{4}.}

\item{label.fg}{Label foreground color. Default \code{"white"}.}

\item{label.bg}{Label background color. Default \code{"black"}.}

\item{label.bg.r}{Label background ratio. Default \code{0.1}.}

\item{label_insitu}{Logical. Place raw group names at cell centers.
Default \code{FALSE}.}

\item{label_repel}{Logical. Repel labels. Default \code{FALSE}.}

\item{label_repulsion}{Repulsion force. Default \code{20}.}

\item{label_point_size}{Center point size for labels. Default \code{1}.}

\item{label_point_color}{Center point color. Default \code{"black"}.}

\item{label_segment_color}{Segment color for repelled labels. Default
\code{"black"}.}

\item{cells.highlight}{Character vector of cell names to highlight, or
\code{TRUE} for all non-NA cells. Default \code{NULL}.}

\item{cols.highlight}{Highlight border color. Default \code{"black"}.}

\item{sizes.highlight}{Highlight point size. Default \code{1}.}

\item{alpha.highlight}{Highlight transparency. Default \code{1}.}

\item{stroke.highlight}{Highlight border width. Default \code{0.5}.}

\item{add_density}{Logical. Add density contours. Default \code{FALSE}.}

\item{density_color}{Contour line color. Default \code{"grey80"}.}

\item{density_filled}{Logical. Use filled contour bands. Default
\code{FALSE}.}

\item{density_filled_palette}{Palette for filled contours. Default
\code{"Greys"}.}

\item{density_filled_palcolor}{Custom colors for filled contours. Default
\code{NULL}.}

\item{add_mark}{Logical. Add group boundary marks. Default \code{FALSE}.}

\item{mark_type}{Mark shape: \code{"hull"}, \code{"ellipse"},
\code{"rect"}, or \code{"circle"}. Default \code{"hull"}.}

\item{mark_expand}{Mark expansion. Default \code{grid::unit(3, "mm")}.}

\item{mark_alpha}{Mark transparency. Default \code{0.1}.}

\item{mark_linetype}{Mark border line type. Default \code{1}.}

\item{lineages}{Character vector of metadata column names containing
pseudotime values (e.g. from \code{RunSlingshot}). Each column is one
lineage; \code{NULL} = no lineage overlay. Default \code{NULL}.}

\item{lineages_trim}{Numeric pair. Quantile range of pseudotime cells to
include in LOESS fit (removes extremes). Default \code{c(0.01, 0.99)}.}

\item{lineages_span}{LOESS smoother span. Larger = smoother. Default
\code{0.75}.}

\item{lineages_palette}{Colour palette for lineage lines. Default
\code{"Dark2"}.}

\item{lineages_palcolor}{Custom colour vector for lineages. Default
\code{NULL}.}

\item{lineages_arrow}{Arrow specification for path end. Default
\code{grid::arrow(length = grid::unit(0.1, "inches"))}.}

\item{lineages_linewidth}{Foreground path linewidth. Default \code{1}.}

\item{lineages_line_bg}{Colour of the outline stroke behind each path.
Default \code{"white"}.}

\item{lineages_line_bg_stroke}{Extra width added to the outline. Default
\code{0.5}.}

\item{lineages_whiskers}{Logical. Draw segments from each cell to the
smooth curve. Default \code{FALSE}.}

\item{lineages_whiskers_linewidth}{Whisker segment linewidth. Default
\code{0.5}.}

\item{lineages_whiskers_alpha}{Whisker transparency. Default \code{0.5}.}

\item{hex}{Logical. Use hexagonal binning. Default \code{FALSE}.}

\item{hex.count}{Logical. Map hex alpha to count. Default \code{TRUE}.}

\item{hex.bins}{Number of hex bins. Default \code{50}.}

\item{hex.binwidth}{Hex bin width. Default \code{NULL}.}

\item{hex.linewidth}{Hex border width. Default \code{0.5}.}

\item{raster}{Logical. Rasterize points (auto if >100k cells). Default
\code{NULL}.}

\item{raster_method}{Character. Rasterisation backend:
\code{"rasterise"} (default, faithful colours via \code{rasterise_layer})
or \code{"scattermore"} (faster C-level rendering, may shift colours).}

\item{raster.dpi}{Numeric. Raster resolution in DPI. Accepts a scalar or
a two-length vector (max is used). Default \code{c(512, 512)}.}

\item{aspect.ratio}{Aspect ratio. Default \code{1}.}

\item{title}{Plot title. Default \code{NULL}.}

\item{subtitle}{Plot subtitle. Default \code{NULL}.}

\item{xlab}{X-axis label. Default auto from reduction key.}

\item{ylab}{Y-axis label. Default auto from reduction key.}

\item{legend.position}{Legend position. Default \code{"right"}.}

\item{legend.direction}{Legend direction. Default \code{"vertical"}.}

\item{legend.title}{Legend title. Default \code{NULL} (uses group name).}

\item{theme_use}{Theme specification. Accepts a function (e.g.
\code{UtilsR::theme_blank}), a character name (e.g. \code{"theme_sc"}),
or a \code{theme} object (e.g. \code{theme_bw()}). Default \code{NULL}
which uses \code{UtilsR::theme_blank}.}

\item{theme_args}{List of extra arguments to \code{theme_use}. Default
\code{list()}.}

\item{combine}{Logical. Combine multiple panels via patchwork. Default
\code{TRUE}.}

\item{nrow, ncol}{Layout dimensions for combined plot. Default \code{NULL}.}

\item{byrow}{Arrange panels by row. Default \code{TRUE}.}

\item{force}{Logical. Force plot even with >100 group levels. Default
\code{FALSE}.}

\item{seed}{Random seed. Default \code{11}.}
}
\value{
A ggplot object (or list of ggplot objects if \code{combine =
  FALSE}).
}
\description{
Visualize cell groups on a 2-dimensional reduction plot (UMAP, t-SNE, etc.).
Supports Seurat objects and plain data.frames. Provides label, highlight,
density, mark, and raster layers with flexible theming.

Ported from \code{scop::CellDimPlot} with code optimizations. Uses
\code{UtilsR} theme / grob utilities and \code{scMMR::palette_colors} for
color mapping.
}
\examples{
\dontrun{
# --- Seurat input ---
DimPlot2(seu, group.by = "celltype")
DimPlot2(seu, group.by = "celltype", label = TRUE, add_mark = TRUE)

# --- data.frame input ---
df <- data.frame(UMAP1 = rnorm(200), UMAP2 = rnorm(200),
                 cluster = sample(letters[1:5], 200, replace = TRUE))
DimPlot2(df, group.by = "cluster", dims = c(1, 2))
}

}
