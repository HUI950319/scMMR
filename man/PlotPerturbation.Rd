% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{PlotPerturbation}
\alias{PlotPerturbation}
\title{Perturbation Ranking Plot}
\usage{
PlotPerturbation(
  rank_result,
  top_k = NULL,
  display = c("lollipop", "bar"),
  sig_threshold = 0.05,
  palette = "Reds 3",
  base_size = 12
)
}
\arguments{
\item{rank_result}{Output list from \code{RankPerturbation()}.}

\item{top_k}{Integer. Show only top-k cell types (default \code{NULL},
show all).}

\item{display}{Character: \code{"lollipop"} (default) or \code{"bar"}.}

\item{sig_threshold}{Numeric. FDR threshold for significance stars
(default 0.05).}

\item{palette}{HCL sequential palette name (default \code{"Reds 3"}).}

\item{base_size}{Base font size (default 12).}
}
\value{
A ggplot object.
}
\description{
Visualise the output of \code{RankPerturbation()}: a ranked lollipop or
bar chart of cell types ordered by perturbation score, with significance
markers.  Reuses the same visual style as \code{PlotImportance()}.
}
\examples{
\dontrun{
res <- RankPerturbation(pred$shared_embedding, q1@meta.data,
                        conditions = c("PH", "SH"))
PlotPerturbation(res)
PlotPerturbation(res, top_k = 10, display = "bar")
}

}
