% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PlotSankey.R
\name{PlotSankey}
\alias{PlotSankey}
\title{Sankey Plot for Cell Population Composition}
\usage{
PlotSankey(
  cellmeta,
  by,
  fill,
  colors = NULL,
  palette = NULL,
  flow.alpha = 0.4,
  bar.width = 0.5,
  bar.color = "gray50",
  show.label = FALSE,
  label.size = 3,
  label.color = "black",
  show.pct = FALSE,
  y.percent = TRUE,
  legend.ncol = 1,
  base.size = 15,
  curve.type = "sigmoid"
)
}
\arguments{
\item{cellmeta}{A Seurat object or data.frame containing cell metadata
(e.g. \code{seurat_obj} or \code{seurat_obj@meta.data}).}

\item{by}{Character string specifying the grouping variable
(e.g. \code{"group"}, \code{"sample"}).}

\item{fill}{Character string specifying the fill variable
(e.g. \code{"cell_type_pred"}).}

\item{colors}{Optional character vector of colours. If \code{NULL} and
\code{palette} is also \code{NULL}, default ggplot2 colours are used.
A named vector maps specific levels to colours.}

\item{palette}{Optional RColorBrewer palette name (e.g. \code{"Paired"},
\code{"Set2"}) for auto-generating colours when \code{colors = NULL}.
Default \code{NULL} (use ggplot2 defaults).}

\item{flow.alpha}{Numeric (0-1), transparency of alluvial flows.
Default 0.4.}

\item{bar.width}{Numeric (0-1), width of stratum bars. Default 0.5.}

\item{bar.color}{Stratum bar border colour. Default \code{"gray50"}.}

\item{show.label}{Logical, whether to show cell type labels inside
strata. Default \code{FALSE}.}

\item{label.size}{Numeric, font size for stratum labels. Default 3.}

\item{label.color}{Stratum label colour. Default \code{"black"}.}

\item{show.pct}{Logical, whether to append within-group percentage to
labels. Default \code{FALSE}. Only used when \code{show.label = TRUE}.}

\item{y.percent}{Logical. If \code{TRUE} (default), y-axis shows
percentages (all bars equal height). If \code{FALSE}, y-axis shows
raw cell counts.}

\item{legend.ncol}{Integer, number of legend columns. Default 1.}

\item{base.size}{Numeric, base font size for the theme. Default 15.}

\item{curve.type}{Character, flow curve type. Default \code{"sigmoid"}.
Other options: \code{"linear"}, \code{"cubic"}, etc.}
}
\value{
A ggplot object.
}
\description{
Create a sankey (alluvial flow) plot showing cell type composition changes
across conditions/groups, based on the ggalluvial package. Each column
represents a group, strata represent cell types, and flows connect the
same cell type across adjacent groups. Parameters mirror
\code{\link{PlotAlluvia}}, with an additional \code{curve.type} argument.
}
\examples{
\dontrun{
# From Seurat object directly
PlotSankey(seurat_obj, by = "group", fill = "celltype")

# From data.frame
PlotSankey(seurat_obj@meta.data, by = "group", fill = "cell_type_pred")
}

}
