% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PlotPercent.R
\name{PlotPercent}
\alias{PlotPercent}
\title{Differential Abundance Beeswarm Plot}
\usage{
PlotPercent(
  da_result,
  fdr_threshold = 0.1,
  colors = c("blue", "white", "red"),
  na_color = "grey80",
  show_boxplot = FALSE,
  base_size = 12,
  point_size = 1.5
)
}
\arguments{
\item{da_result}{Output list from \code{RankPercent()}.}

\item{fdr_threshold}{Numeric. FDR threshold for colouring significant
neighborhoods (default 0.1).  Neighborhoods with
\code{p_adj >= fdr_threshold} are shown in \code{na_color}.}

\item{colors}{Character vector of length 3: colours for the diverging
gradient \code{c(low, mid, high)}.  Default
\code{c("blue", "white", "red")}.}

\item{na_color}{Colour for non-significant neighborhoods.
Default \code{"grey80"}.}

\item{show_boxplot}{Logical. If \code{TRUE}, overlay a transparent
boxplot behind the beeswarm points to show the distribution summary
for each cell type. Default \code{FALSE}.}

\item{base_size}{Base font size (default 12).}

\item{point_size}{Point size (default 1.5).}
}
\value{
A ggplot object.
}
\description{
Visualise the output of \code{RankPercent()}: a miloR-style beeswarm plot
showing per-neighborhood logFC for each cell type.  Significant
neighborhoods (FDR < threshold) are coloured by their logFC value on a
diverging blue-white-red gradient; non-significant neighborhoods are
shown in grey.
}
\examples{
\dontrun{
da <- RankPercent(pred$shared_embedding, q1@meta.data,
                  conditions = c("PH", "SH"))
PlotPercent(da)
PlotPercent(da, fdr_threshold = 0.05)
}

}
