% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gsea_enrich.R
\name{CrossEnrichOverlap}
\alias{CrossEnrichOverlap}
\title{Cross-comparison of Core Enrichment Genes between Two GSEA Results}
\usage{
CrossEnrichOverlap(
  res_tf,
  res_pathway,
  type = c("gsea", "enricher"),
  p.cutoff = 0.05,
  min.overlap = 1,
  show.genes = TRUE,
  plot = TRUE,
  top.tf = 20,
  top.pathway = 20
)
}
\arguments{
\item{res_tf}{TF/GRN \code{RunGseaEnrich} result (or \code{compareClusterResult}).}

\item{res_pathway}{Pathway \code{RunGseaEnrich} result (or \code{compareClusterResult}).}

\item{type}{Which sub-result to use: \code{"gsea"} or \code{"enricher"}.
Default: \code{"gsea"}.}

\item{p.cutoff}{P.adjust cutoff for filtering enriched terms. Default: 0.05.}

\item{min.overlap}{Minimum number of overlapping genes to report. Default: 1.}

\item{show.genes}{Logical; include overlapping gene names in result.
Default: \code{TRUE}.}

\item{plot}{Logical; generate a tile heatmap of overlap counts.
Default: \code{TRUE}.}

\item{top.tf}{Maximum number of TF terms to display (by overlap count).
Default: 20.}

\item{top.pathway}{Maximum number of pathway terms to display.
Default: 20.}
}
\value{
A list with:
  \describe{
    \item{\code{overlap}}{Data.frame with columns: \code{TF}, \code{Pathway},
      \code{n_overlap}, \code{overlap_genes}, \code{n_tf_genes},
      \code{n_pathway_genes}, \code{jaccard}.}
    \item{\code{tf_genes}}{Named list of core_enrichment genes per TF term.}
    \item{\code{pathway_genes}}{Named list of core_enrichment genes per
      pathway term.}
    \item{\code{plot}}{(if \code{plot = TRUE}) A ggplot tile plot of overlap
      counts.}
  }
}
\description{
Compare core enrichment genes between two \code{RunGseaEnrich} results
(e.g., TF regulons vs pathways) to identify shared regulatory genes.
Returns an overlap summary table and optionally a heatmap or tile plot.
}
\examples{
\dontrun{
# Compare TF regulon enrichment with pathway enrichment
ov <- CrossEnrichOverlap(res_PH_TF, res_PH)
head(ov$overlap)
ov$plot

# Only enricher results, stricter cutoff
ov2 <- CrossEnrichOverlap(res_TF, res_pathway,
  type = "enricher", p.cutoff = 0.01, min.overlap = 3)
}

}
