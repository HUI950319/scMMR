% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_module_score.R
\name{ComputeModuleScore}
\alias{ComputeModuleScore}
\alias{ComputeModuleScore.default}
\alias{ComputeModuleScore.Seurat}
\title{Calculate gene module scores}
\usage{
ComputeModuleScore(x, ...)

\method{ComputeModuleScore}{default}(
  x,
  gene.sets,
  method = c("AUCell", "Seurat", "UCell"),
  min.size = 20,
  batch.size = 500,
  nbin = 24,
  ctrl = 100,
  cores = 1,
  seed = 11,
  ...
)

\method{ComputeModuleScore}{Seurat}(
  x,
  gene.sets,
  method = c("AUCell", "Seurat", "UCell"),
  min.size = 20,
  batch.size = 500,
  nbin = 24,
  ctrl = 100,
  cores = 1,
  seed = 11,
  assay = Seurat::DefaultAssay(x),
  layer = NULL,
  store = c("assay", "metadata"),
  assay.name = NULL,
  prefix = NULL,
  ...
)
}
\arguments{
\item{x}{A gene expression matrix (genes x cells) or a Seurat object.}

\item{...}{Additional arguments passed to scoring methods.}

\item{gene.sets}{Gene sets in one of three formats:
\itemize{
  \item A named list of character vectors (gene names).
  \item A data.frame with at least two columns: term (gene set name) and gene (gene symbol).
  \item A file path to a GMT file.
}}

\item{method}{Scoring method: \code{"AUCell"} (default), \code{"Seurat"}, or \code{"UCell"}.}

\item{min.size}{Minimum number of genes (after filtering) for a gene set to be scored. Default: 20.}

\item{batch.size}{Number of cells per batch for AUCell method to reduce memory. Default: 500.}

\item{nbin}{Number of expression bins for Seurat method control gene selection. Default: 24.}

\item{ctrl}{Number of control genes per feature gene for Seurat method. Default: 100.}

\item{cores}{Number of parallel cores. Default: 1.}

\item{seed}{Random seed for reproducibility. Default: 11.}

\item{assay}{Name of the Seurat assay to use. Default: \code{DefaultAssay(x)}.}

\item{layer}{Data layer to extract: \code{"counts"} or \code{"data"}.
Defaults to \code{"counts"} for AUCell/UCell and \code{"data"} for Seurat method.}

\item{store}{Storage destination for scores:
\code{"assay"} (default) stores as a new assay,
\code{"metadata"} stores as columns in \code{meta.data}.}

\item{assay.name}{Name of the new assay when \code{store = "assay"}. Default: the method name.}

\item{prefix}{Column name prefix when \code{store = "metadata"}. Default: the method name
followed by underscore (e.g. \code{"AUCell_setA"}).}
}
\value{
A score matrix (gene sets x cells) or a Seurat object with scores stored as an assay.
}
\description{
Compute gene module activity scores using AUCell, Seurat (AddModuleScore), or UCell methods.
Supports multiple input formats for gene sets: named list, data.frame, or GMT file.
}
