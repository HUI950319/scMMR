% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate.R
\name{PlotEmbeddingEval}
\alias{PlotEmbeddingEval}
\title{Plot Embedding Evaluation Results}
\usage{
PlotEmbeddingEval(eval_result, which = "all", base_size = 12)
}
\arguments{
\item{eval_result}{Output from \code{EvaluateEmbedding()}.}

\item{which}{Character. Which plot(s) to draw:
\code{"all"} (default, returns a list of all available plots),
\code{"elbow"}, \code{"cumvar"}, \code{"knn"}, \code{"distcor"},
or \code{"silhouette"}.}

\item{base_size}{Numeric. Base font size (default 12).}
}
\value{
A single ggplot object or a named list of ggplot objects
  (when \code{which = "all"}).
}
\description{
Visualise results from \code{\link{EvaluateEmbedding}}.
}
\examples{
\dontrun{
eval_res <- EvaluateEmbedding(pred$shared_embedding, seurat_obj = seu)

# All plots as a list
plots <- PlotEmbeddingEval(eval_res)
plots$elbow
plots$knn

# Single plot
PlotEmbeddingEval(eval_res, which = "silhouette")

# Combine with patchwork
library(patchwork)
plots$elbow + plots$cumvar + plots$knn + plots$distcor
}

}
