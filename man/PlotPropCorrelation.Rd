% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PlotPropCorrelation.R
\name{PlotPropCorrelation}
\alias{PlotPropCorrelation}
\title{Cell-Type Proportion vs Gene Expression Correlation}
\usage{
PlotPropCorrelation(
  seu,
  gene,
  celltype,
  celltype.col,
  sample.col,
  expr.in = NULL,
  group.by = NULL,
  assay = NULL,
  layer = "data",
  method = c("spearman", "pearson", "kendall"),
  show.cor = TRUE,
  show.smooth = TRUE,
  smooth.method = "lm",
  point.size = 3,
  point.alpha = 0.7,
  point.color = "#984ea3",
  cor.size = 4,
  marginal = "none",
  marginal.size = 5,
  palette = "Paired",
  palcolor = NULL,
  title = NULL,
  ncol = 3L,
  return.data = FALSE
)
}
\arguments{
\item{seu}{A Seurat object.}

\item{gene}{Character vector. Gene(s) whose mean expression per sample is
plotted on the y-axis.  When length > 1 the plot is faceted.}

\item{celltype}{Character vector. Cell type(s) whose proportion per sample
is plotted on the x-axis.  When length > 1 the plot is faceted.}

\item{celltype.col}{Character. Column in \code{meta.data} containing cell
type annotations.}

\item{sample.col}{Character. Column in \code{meta.data} containing sample /
patient identifiers.}

\item{expr.in}{Character vector or \code{NULL}. Cell types in which to
average gene expression.  \code{NULL} (default) = all cells in each
sample.  Set to e.g. \code{"Parathyroid"} to average only within that
cell type.}

\item{group.by}{Character or \code{NULL}. Metadata column for coloring
points (e.g. \code{"disease"}).  Each sample must have a unique group
label. Default: \code{NULL}.}

\item{assay}{Character. Seurat assay. Default: \code{DefaultAssay(seu)}.}

\item{layer}{Character. Data layer. Default: \code{"data"}.}

\item{method}{Correlation method: \code{"spearman"}, \code{"pearson"}, or
\code{"kendall"}. Default: \code{"spearman"}.}

\item{show.cor}{Logical. Show correlation statistics. Default: \code{TRUE}.}

\item{show.smooth}{Logical. Show regression line. Default: \code{TRUE}.}

\item{smooth.method}{Smoothing method for the trend line. Default:
\code{"lm"}.}

\item{point.size}{Numeric. Point size. Default: 3.}

\item{point.alpha}{Numeric. Point transparency. Default: 0.7.}

\item{point.color}{Character. Point color when \code{group.by = NULL}.
Default: \code{"#984ea3"}.}

\item{cor.size}{Numeric. Font size for correlation text. Default: 4.}

\item{marginal}{Character. Marginal plot type passed to
\code{\link{PlotScatter}}: \code{"none"}, \code{"density"},
\code{"histogram"}, etc.  Default: \code{"none"}.}

\item{marginal.size}{Numeric. Relative size of marginal plots. Default: 5.}

\item{palette}{Character. Palette name. Default: \code{"Paired"}.}

\item{palcolor}{Character vector or \code{NULL}. Custom colors.}

\item{title}{Character or \code{NULL}. Plot title.}

\item{ncol}{Integer. Facet columns. Default: 3.}

\item{return.data}{Logical. If \code{TRUE}, return the sample-level
data.frame instead of the plot.  Default: \code{FALSE}.}
}
\value{
A \code{ggplot} / \code{patchwork} object, or a data.frame if
  \code{return.data = TRUE}.
}
\description{
For each sample, compute the proportion of a given cell type and the
average expression of one or more genes, then draw a scatter-correlation
plot.  Internally delegates to \code{\link{PlotScatter}}, so all its
features (grouping, marginal plots, regression lines, etc.) are available.
}
\examples{
\dontrun{
# Basic: Parathyroid proportion vs PTH expression
PlotPropCorrelation(seu, gene = "PTH", celltype = "Parathyroid",
                    celltype.col = "celltype", sample.col = "sample_id")

# Color by disease group
PlotPropCorrelation(seu, gene = "PTH", celltype = "Parathyroid",
                    celltype.col = "celltype", sample.col = "sample_id",
                    group.by = "disease")

# Multiple cell types
PlotPropCorrelation(seu, gene = "PTH",
                    celltype = c("Parathyroid", "Stromal"),
                    celltype.col = "celltype", sample.col = "sample_id")

# Multiple genes
PlotPropCorrelation(seu, gene = c("PTH", "GCM2"),
                    celltype = "Parathyroid",
                    celltype.col = "celltype", sample.col = "sample_id",
                    marginal = "density")

# Return data for custom plotting
df <- PlotPropCorrelation(seu, gene = "PTH", celltype = "Parathyroid",
                          celltype.col = "celltype",
                          sample.col = "sample_id",
                          return.data = TRUE)
}

}
\seealso{
\code{\link{PlotScatter}}
}
