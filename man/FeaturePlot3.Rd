% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FeaturePlot3.R
\name{FeaturePlot3}
\alias{FeaturePlot3}
\title{Expression distribution plots for Seurat objects}
\usage{
FeaturePlot3(
  seu,
  features,
  group.by = NULL,
  split.by = NULL,
  bg.by = NULL,
  plot.by = c("group", "feature"),
  fill.by = c("group", "feature", "expression"),
  cells = NULL,
  assay = NULL,
  layer = "data",
  keep_empty = FALSE,
  individual = FALSE,
  type = c("violin", "box", "bar", "dot", "col"),
  palette = "npg",
  palcolor = NULL,
  alpha = 1,
  bg_palette = "Set2",
  bg_palcolor = NULL,
  bg_alpha = 0.2,
  add_box = FALSE,
  box_color = "black",
  box_width = 0.1,
  box_ptsize = 2,
  add_point = FALSE,
  pt.color = "grey30",
  pt.size = NULL,
  pt.alpha = 1,
  jitter.width = 0.4,
  jitter.height = 0.1,
  add_trend = FALSE,
  trend_color = "black",
  trend_linewidth = 1,
  trend_ptsize = 2,
  add_stat = c("none", "mean", "median"),
  stat_color = "black",
  stat_size = 1,
  stat_stroke = 1,
  stat_shape = 25,
  add_line = NULL,
  line_color = "red",
  line_size = 1,
  line_type = 1,
  cells.highlight = NULL,
  cols.highlight = "red",
  sizes.highlight = 1,
  alpha.highlight = 1,
  calculate_coexp = FALSE,
  same.y.lims = FALSE,
  y.min = NULL,
  y.max = NULL,
  y.trans = "identity",
  y.nbreaks = 5,
  sort = FALSE,
  stack = FALSE,
  flip = FALSE,
  comparisons = NULL,
  ref_group = NULL,
  pairwise_method = "wilcox.test",
  multiplegroup_comparisons = FALSE,
  multiple_method = "kruskal.test",
  sig_label = c("p.signif", "p.format"),
  sig_labelsize = 3.5,
  aspect.ratio = NULL,
  title = NULL,
  subtitle = NULL,
  xlab = NULL,
  ylab = "Expression level",
  legend.position = "right",
  legend.direction = "vertical",
  legend.title = NULL,
  theme_use = NULL,
  theme_args = list(),
  combine = TRUE,
  nrow = NULL,
  ncol = NULL,
  byrow = TRUE,
  force = FALSE,
  seed = 11
)
}
\arguments{
\item{seu}{A Seurat object or data.frame. When a data.frame is provided,
\code{assay} and \code{layer} are ignored.}

\item{features}{Character vector of feature names (genes or numeric metadata
columns).}

\item{group.by}{Column name for the x-axis grouping variable. \code{NULL}
treats all cells as a single group.}

\item{split.by}{Column name for a secondary grouping (dodging within each
x-axis position). Default \code{NULL}.}

\item{bg.by}{Column name whose levels define background colour bands.
Must be a super-grouping of \code{group.by} (each \code{group.by} level
belongs to exactly one \code{bg.by} level). Default \code{NULL}.}

\item{plot.by}{Axis orientation: \code{"group"} (default) places groups on
the x-axis (one panel per feature); \code{"feature"} places features on
the x-axis (one panel per group level).}

\item{fill.by}{What drives fill colour:
\itemize{
  \item \code{"group"} (default) — group or split levels.
  \item \code{"feature"} — feature name.
  \item \code{"expression"} — median expression per group (continuous
    gradient).
}}

\item{cells}{Character vector of cell barcodes to include. \code{NULL} = all.}

\item{assay}{Seurat assay name. \code{NULL} = active assay.}

\item{layer}{Seurat layer. Default \code{"data"}.}

\item{keep_empty}{Logical. Keep empty factor levels on the x-axis. Default
\code{FALSE}.}

\item{individual}{Logical. Create a separate panel for each group level ×
feature × split level combination. Default \code{FALSE}.}

\item{type}{Plot geometry. One of \code{"violin"} (default), \code{"box"},
\code{"bar"}, \code{"dot"}, or \code{"col"} (per-cell column bar).}

\item{palette}{Colour palette name for fill colours. Default \code{"npg"}.}

\item{palcolor}{Custom colour vector. Overrides \code{palette}. Default
\code{NULL}.}

\item{alpha}{Fill transparency (0–1). Default \code{1}.}

\item{bg_palette}{Colour palette for background bands. Default \code{"Set2"}.}

\item{bg_palcolor}{Custom colours for background bands. Default \code{NULL}.}

\item{bg_alpha}{Transparency of background bands. Default \code{0.2}.}

\item{add_box}{Logical. Overlay a narrow box-plot (violin/bar only). Default
\code{FALSE}.}

\item{box_color}{Colour of overlay box-plot. Default \code{"black"}.}

\item{box_width}{Width of overlay box-plot. Default \code{0.1}.}

\item{box_ptsize}{Median point size in box-plot. Default \code{2}.}

\item{add_point}{Logical. Overlay jittered individual points. Default
\code{FALSE}.}

\item{pt.color}{Point colour. Default \code{"grey30"}.}

\item{pt.size}{Point size. \code{NULL} = auto.}

\item{pt.alpha}{Point transparency. Default \code{1}.}

\item{jitter.width}{Horizontal jitter. Default \code{0.4}.}

\item{jitter.height}{Vertical jitter. Default \code{0.1}.}

\item{add_trend}{Logical. Connect group medians/means with a trend line
(violin/box: median; bar: mean). Default \code{FALSE}.}

\item{trend_color}{Trend line colour. Default \code{"black"}.}

\item{trend_linewidth}{Trend line width. Default \code{1}.}

\item{trend_ptsize}{Trend point size. Default \code{2}.}

\item{add_stat}{Summary stat to overlay. One of \code{"none"} (default),
\code{"mean"}, or \code{"median"}.}

\item{stat_color}{Stat point colour. Default \code{"black"}.}

\item{stat_size}{Stat point size. Default \code{1}.}

\item{stat_stroke}{Stat point stroke. Default \code{1}.}

\item{stat_shape}{Stat point shape (integer). Default \code{25} (filled
downward triangle).}

\item{add_line}{Numeric y-intercept for a horizontal reference line.
\code{NULL} = none.}

\item{line_color}{Reference line colour. Default \code{"red"}.}

\item{line_size}{Reference line width. Default \code{1}.}

\item{line_type}{Reference line type. Default \code{1} (solid).}

\item{cells.highlight}{Character vector of cell barcodes to highlight, or
\code{TRUE} for all cells. Requires \code{add_point = TRUE}. Default
\code{NULL}.}

\item{cols.highlight}{Highlight point colour. Default \code{"red"}.}

\item{sizes.highlight}{Highlight point size. Default \code{1}.}

\item{alpha.highlight}{Highlight point transparency. Default \code{1}.}

\item{calculate_coexp}{Logical. Compute geometric mean co-expression of all
\code{features} and add it as an extra \code{"CoExp"} panel. Only valid
when all features are genes (in the expression matrix). Default
\code{FALSE}.}

\item{same.y.lims}{Logical. Share y-axis limits across all panels. Default
\code{FALSE}.}

\item{y.min}{Minimum y limit: numeric value or quantile string (e.g.
\code{"q1"}). \code{NULL} = data minimum.}

\item{y.max}{Maximum y limit: numeric value or quantile string (e.g.
\code{"q99"}). \code{NULL} = data maximum.}

\item{y.trans}{Y-axis transformation. \code{"identity"} (default) or
\code{"log2"}.}

\item{y.nbreaks}{Number of y-axis tick breaks. Default \code{5}.}

\item{sort}{Logical or \code{"increasing"} / \code{"decreasing"}. Sort
groups by median of the first feature. Default \code{FALSE}.}

\item{stack}{Logical. Stack all feature panels vertically (or horizontally
when \code{flip = TRUE}) with shared axes and a single shared legend.
Default \code{FALSE}.}

\item{flip}{Logical. Flip coordinates (horizontal layout). Default
\code{FALSE}.}

\item{comparisons}{List of length-2 character vectors specifying pairwise
comparisons, or \code{TRUE} to auto-detect groups from \code{split.by}.
\code{NULL} = none.}

\item{ref_group}{Reference group for one-vs-all comparisons. Default
\code{NULL}.}

\item{pairwise_method}{Pairwise test. Default \code{"wilcox.test"}.}

\item{multiplegroup_comparisons}{Logical. Add an overall group test label.
Default \code{FALSE}.}

\item{multiple_method}{Overall group test. Default \code{"kruskal.test"}.}

\item{sig_label}{Significance label format: \code{"p.signif"} (default) or
\code{"p.format"}.}

\item{sig_labelsize}{Text size of significance labels. Default \code{3.5}.}

\item{aspect.ratio}{Panel aspect ratio. \code{NULL} = free.}

\item{title}{Plot title. \code{NULL} = none.}

\item{subtitle}{Plot subtitle. \code{NULL} = none.}

\item{xlab}{X-axis label. \code{NULL} = auto (\code{group.by}).}

\item{ylab}{Y-axis label. Default \code{"Expression level"}.}

\item{legend.position}{Legend position string or coordinates. Default
\code{"right"}.}

\item{legend.direction}{Legend direction. Default \code{"vertical"}.}

\item{legend.title}{Custom legend title. \code{NULL} = auto.}

\item{theme_use}{Theme specification. \code{NULL} = \code{ggplot2::theme_bw};
accepts a character function name, a function, or a \code{theme} object.}

\item{theme_args}{List of extra arguments passed to \code{theme_use}. Default
\code{list()}.}

\item{combine}{Logical. Combine panels into a single patchwork. Default
\code{TRUE}.}

\item{nrow}{Number of rows in patchwork layout.}

\item{ncol}{Number of columns in patchwork layout.}

\item{byrow}{Logical. Fill patchwork by row. Default \code{TRUE}.}

\item{force}{Logical. Skip the >100-level cardinality check. Default
\code{FALSE}.}

\item{seed}{Random seed for jitter. Default \code{11}.}
}
\value{
A ggplot2 / patchwork object (or list when \code{combine = FALSE}).
}
\description{
Generates violin, box, bar, dot, or column plots for one or more features
(genes or metadata numeric columns) stratified by a grouping variable.
Full feature parity with \code{scop::FeatureStatPlot} with optimised
parameter names consistent with \code{DimPlot2} / \code{FeaturePlot2}.
}
\examples{
\dontrun{
# Basic violin plot
FeaturePlot3(seu, features = c("CD3D", "CD8A"), group.by = "cell_type")

# Box plot with statistical comparisons
FeaturePlot3(seu, features = "MS4A1", group.by = "cluster",
             type = "box", comparisons = list(c("B", "T")))

# Features on x-axis (one panel per cell type)
FeaturePlot3(seu, features = c("CD3D", "CD8A", "MS4A1"),
             group.by = "cell_type", plot.by = "feature")

# Stacked panels with background bands
FeaturePlot3(seu,
  features = c("Gene1", "Gene2", "Gene3"),
  group.by = "SubCellType", bg.by = "CellType",
  stack = TRUE, add_box = TRUE)

# Col (per-cell bar) plot
FeaturePlot3(seu, features = "CD3D", group.by = "cell_type", type = "col")
}

}
