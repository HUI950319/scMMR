% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rank.R
\name{RankPercent}
\alias{RankPercent}
\title{Differential Abundance Testing via KNN Neighborhoods}
\usage{
RankPercent(
  embedding,
  cell_meta,
  cell_type_col = "cell_type_pred",
  condition_col = "group",
  conditions = NULL,
  k = 30L,
  prop_sample = 0.1,
  test = c("fisher", "nb_glm"),
  min_cells = 20L,
  fdr_threshold = 0.1,
  seed = 42L,
  verbose = TRUE
)
}
\arguments{
\item{embedding}{Numeric matrix (n_cells x d), typically
\code{pred$shared_embedding}.}

\item{cell_meta}{A data.frame. Rownames must be cell IDs.}

\item{cell_type_col}{Column name for cell type labels
(default \code{"cell_type_pred"}).}

\item{condition_col}{Column name for condition/group labels
(default \code{"group"}).}

\item{conditions}{Optional character vector of length 2. If provided,
only these two conditions are tested. If \code{NULL} (default), all
conditions are used; Fisher's or chi-squared test is applied.}

\item{k}{Number of nearest neighbors (default 30).}

\item{prop_sample}{Proportion of cells sampled as neighborhood centers
(default 0.1).}

\item{test}{Statistical test: \code{"fisher"} (default) or
\code{"nb_glm"} (negative binomial GLM via \code{MASS::glm.nb}).}

\item{min_cells}{Minimum cells in a neighborhood to include
(default 20).}

\item{fdr_threshold}{FDR threshold for summary (default 0.1).}

\item{seed}{Random seed (default 42).}

\item{verbose}{Logical. Print progress messages (default \code{TRUE}).}
}
\value{
A named list:
  \describe{
    \item{da_results}{data.frame: nhood_idx, nhood_cell_id, n_cells,
      counts per condition, logFC, p_value, p_adj,
      cell_type_majority.}
    \item{cell_da_scores}{Named numeric vector of per-cell DA scores.
      Ready for \code{Seurat::AddMetaData()}.}
    \item{cell_type_summary}{data.frame: cell_type, n_nhoods,
      n_da_nhoods, mean_logFC, fraction_da.}
    \item{params}{List of parameters used.}
  }
}
\description{
Builds a KNN graph on the DNN's shared embedding, samples representative
neighborhoods, and tests for differential cell abundance between
conditions. Inspired by the Milo framework but operating on DNN-learned
representations.
}
\examples{
\dontrun{
pred <- DNN_predict(query, model_path, return_embedding = TRUE)
q1 <- Seurat::AddMetaData(toy_test, pred$predictions)
da <- RankPercent(pred$shared_embedding, q1@meta.data,
                  conditions = c("PH", "SH"), k = 30)
q1 <- Seurat::AddMetaData(q1, da$cell_da_scores, col.name = "da_score")
PlotPercent(da)
}

}
