% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FeaturePlot2.R
\name{FeaturePlot2}
\alias{FeaturePlot2}
\title{Feature Expression on 2-D Reduction Plot}
\usage{
FeaturePlot2(
  seu,
  features,
  reduction = NULL,
  dims = c(1, 2),
  split.by = NULL,
  cells = NULL,
  layer = "data",
  assay = NULL,
  show_stat = NULL,
  palette = NULL,
  palcolor = NULL,
  pt.size = NULL,
  pt.alpha = 1,
  bg_cutoff = 0,
  bg_color = "grey80",
  keep_scale = "feature",
  lower_quantile = 0,
  upper_quantile = 0.99,
  lower_cutoff = NULL,
  upper_cutoff = NULL,
  add_density = FALSE,
  density_color = "grey80",
  density_filled = FALSE,
  density_filled_palette = "Greys",
  density_filled_palcolor = NULL,
  cells.highlight = NULL,
  cols.highlight = "black",
  sizes.highlight = 1,
  alpha.highlight = 1,
  stroke.highlight = 0.5,
  calculate_coexp = FALSE,
  compare_features = FALSE,
  color_blend_mode = c("blend", "average", "screen", "multiply"),
  label = FALSE,
  label.size = 4,
  label.fg = "white",
  label.bg = "black",
  label.bg.r = 0.1,
  label_insitu = FALSE,
  label_repel = FALSE,
  label_repulsion = 20,
  label_point_size = 1,
  label_point_color = "black",
  label_segment_color = "black",
  lineages = NULL,
  lineages_trim = c(0.01, 0.99),
  lineages_span = 0.75,
  lineages_palette = "Dark2",
  lineages_palcolor = NULL,
  lineages_arrow = grid::arrow(length = grid::unit(0.1, "inches")),
  lineages_linewidth = 1,
  lineages_line_bg = "white",
  lineages_line_bg_stroke = 0.5,
  lineages_whiskers = FALSE,
  lineages_whiskers_linewidth = 0.5,
  lineages_whiskers_alpha = 0.5,
  raster = NULL,
  raster_method = c("scattermore", "rasterise"),
  raster.dpi = c(512, 512),
  aspect.ratio = 1,
  title = NULL,
  subtitle = NULL,
  xlab = NULL,
  ylab = NULL,
  legend.position = "right",
  legend.direction = "vertical",
  legend.title = NULL,
  theme_use = NULL,
  theme_args = list(),
  combine = TRUE,
  nrow = NULL,
  ncol = NULL,
  byrow = TRUE,
  force = FALSE,
  seed = 11
)
}
\arguments{
\item{seu}{A Seurat object or a \code{data.frame}.  For data.frames the
columns specified by \code{dims} are used as x/y coordinates; all feature
columns must also be present in the data.frame.}

\item{features}{Character vector (or named character vector) of features to
plot.  Features can be gene names (Seurat assay rows), metadata columns,
reduction embedding columns, or data.frame columns.  Named features use
the name as subtitle.  A list is also accepted and is flattened.}

\item{reduction}{Character. Reduction to use for coordinates.  \code{NULL}
auto-detects UMAP/tSNE.  Ignored for data.frame input.}

\item{dims}{Integer vector of length 2.  Dimension indices within the
reduction (Seurat) or column indices (data.frame).  Default \code{c(1,2)}.}

\item{split.by}{Column name to split into facets.  \code{NULL} = no split.}

\item{cells}{Character vector of cell/row names to include.  \code{NULL} =
all.}

\item{layer}{Seurat assay layer to use.  Default \code{"data"}.}

\item{assay}{Seurat assay name.  \code{NULL} = default assay.}

\item{show_stat}{Logical.  Show positive-cell statistics in subtitle.
\code{NULL} = auto (\code{FALSE} for blank theme).}

\item{palette}{Colour palette name for the gradient.  Default
\code{"Spectral"} (standard) or \code{"Set1"} (compare mode).}

\item{palcolor}{Custom colour vector.  \code{NULL} = use palette.}

\item{pt.size}{Point size.  \code{NULL} = auto.}

\item{pt.alpha}{Point transparency.  Default \code{1}.}

\item{bg_cutoff}{Numeric.  Feature values ≤ cutoff are treated as
background (shown in \code{bg_color}).  Default \code{0}.}

\item{bg_color}{Background colour for low/NA values.  Default
\code{"grey80"}.}

\item{keep_scale}{How to align colour scales across panels:
\itemize{
\item \code{NULL} – each panel scaled independently to its own range.
\item \code{"feature"} (default) – panels for the same feature share a
scale.
\item \code{"all"} – all panels share a single global scale.
}}

\item{lower_quantile, upper_quantile}{Quantile bounds for colour scale.
Default \code{0} / \code{0.99}.}

\item{lower_cutoff, upper_cutoff}{Explicit value bounds (override quantile).
\code{NULL} = use quantiles.}

\item{add_density}{Logical.  Overlay 2-D density contour.  Default
\code{FALSE}.}

\item{density_color}{Contour colour.  Default \code{"grey80"}.}

\item{density_filled}{Logical.  Filled density raster.  Default
\code{FALSE}.}

\item{density_filled_palette}{Palette for filled density.  Default
\code{"Greys"}.}

\item{density_filled_palcolor}{Custom colours for filled density.
\code{NULL}.}

\item{cells.highlight}{Cells to highlight (names or \code{TRUE} for all
expressed cells).  \code{NULL} = none.}

\item{cols.highlight}{Border colour of highlighted cells.  Default
\code{"black"}.}

\item{sizes.highlight}{Size of highlighted points.  Default \code{1}.}

\item{alpha.highlight}{Alpha of highlighted points.  Default \code{1}.}

\item{stroke.highlight}{Border width around highlighted points.  Default
\code{0.5}.}

\item{calculate_coexp}{Logical.  Replace gene features with their geometric
mean co-expression value.  Default \code{FALSE}.}

\item{compare_features}{Logical.  Blend all features per cell into one
colour.  Default \code{FALSE}.}

\item{color_blend_mode}{Blend mode when \code{compare_features = TRUE}.
One of \code{"blend"}, \code{"average"}, \code{"screen"},
\code{"multiply"}.}

\item{label}{Logical.  Label the high-expression region.  Default
\code{FALSE}.}

\item{label.size}{Label text size.  Default \code{4}.}

\item{label.fg}{Label foreground colour.  Default \code{"white"}.}

\item{label.bg}{Label background colour.  Default \code{"black"}.}

\item{label.bg.r}{Label background ratio.  Default \code{0.1}.}

\item{label_insitu}{Logical.  Use feature names as labels (insitu) instead
of numbers.  Default \code{FALSE}.}

\item{label_repel}{Logical.  Use \code{ggrepel} for labels.  Default
\code{FALSE}.}

\item{label_repulsion}{Repulsion force.  Default \code{20}.}

\item{label_point_size}{Center point size for repelled labels.  Default
\code{1}.}

\item{label_point_color}{Center point colour.  Default \code{"black"}.}

\item{label_segment_color}{Segment colour.  Default \code{"black"}.}

\item{lineages}{Character vector of metadata column names containing
pseudotime values (e.g. from \code{RunSlingshot}).  \code{NULL} = none.}

\item{lineages_trim}{Quantile range for LOESS fit. Default \code{c(0.01,0.99)}.}

\item{lineages_span}{LOESS span. Default \code{0.75}.}

\item{lineages_palette}{Palette for lineage colours. Default \code{"Dark2"}.}

\item{lineages_palcolor}{Custom colour vector. Default \code{NULL}.}

\item{lineages_arrow}{Arrow for path end. Default
\code{grid::arrow(length = grid::unit(0.1, "inches"))}.}

\item{lineages_linewidth}{Foreground linewidth. Default \code{1}.}

\item{lineages_line_bg}{Outline colour. Default \code{"white"}.}

\item{lineages_line_bg_stroke}{Outline extra width. Default \code{0.5}.}

\item{lineages_whiskers}{Draw cell-to-curve segments. Default \code{FALSE}.}

\item{lineages_whiskers_linewidth}{Whisker linewidth. Default \code{0.5}.}

\item{lineages_whiskers_alpha}{Whisker alpha. Default \code{0.5}.}

\item{raster}{Logical.  Rasterise points.  \code{NULL} = auto (> 100k).}

\item{raster_method}{Character. Rasterisation backend:
\code{"rasterise"} (default, faithful colours via \code{rasterise_layer})
or \code{"scattermore"} (faster C-level rendering, may shift colours).}

\item{raster.dpi}{Numeric scalar or 2-length vector.  Default
\code{c(512,512)}.}

\item{aspect.ratio}{Aspect ratio.  Default \code{1}.}

\item{title}{Plot title.  \code{NULL} = none.}

\item{subtitle}{Subtitle(s).  \code{NULL} = auto from feature names.
Single value or vector of \code{length(features)}.}

\item{xlab, ylab}{Axis labels.  \code{NULL} = auto.}

\item{legend.position}{Legend position.  Default \code{"right"}.}

\item{legend.direction}{Legend direction.  Default \code{"vertical"}.}

\item{legend.title}{Legend title.  \code{NULL} = feature name.}

\item{theme_use}{Theme specification (function, character, or
\code{theme} object).  \code{NULL} → \code{UtilsR::theme_blank}.}

\item{theme_args}{Extra arguments for \code{theme_use}.  Default
\code{list()}.}

\item{combine}{Logical.  Combine panels with patchwork.  Default
\code{TRUE}.}

\item{nrow, ncol}{Layout dimensions.  \code{NULL} = auto.}

\item{byrow}{Logical.  Fill by row.  Default \code{TRUE}.}

\item{force}{Logical.  Force plot when > 50 features.  Default
\code{FALSE}.}

\item{seed}{Random seed.  Default \code{11}.}
}
\value{
A ggplot or patchwork object (or list when
\code{combine = FALSE}).
}
\description{
Visualise feature values (gene expression, metadata scores, reduction
embeddings) on a 2-D reduction plot.  Accepts Seurat objects or plain
data.frames.  Key options include:
\itemize{
\item \strong{Multiple features / split panels} with unified colour scale
(\code{keep_scale}).
\item \strong{Co-expression} geometric mean across gene features
(\code{calculate_coexp = TRUE}).
\item \strong{Compare mode} – blend all features per cell into a single colour
(\code{compare_features = TRUE}).
}
}
\examples{
\dontrun{
# Seurat input
FeaturePlot2(seu, features = "CD3D")
FeaturePlot2(seu, features = c("CD3D", "CD8A"), keep_scale = "all")
FeaturePlot2(seu, features = c("CD3D", "CD8A"),
             compare_features = TRUE, color_blend_mode = "blend")

# data.frame input
df <- data.frame(UMAP1 = rnorm(500), UMAP2 = rnorm(500),
                 GeneA = abs(rnorm(500)), GeneB = abs(rnorm(500)))
FeaturePlot2(df, features = "GeneA", dims = c(1, 2))
}

}
