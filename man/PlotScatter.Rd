% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PlotScatter.R
\name{PlotScatter}
\alias{PlotScatter}
\title{Scatter Correlation Plot}
\usage{
PlotScatter(
  object,
  var1,
  var2,
  group.by = NULL,
  split.by = NULL,
  cells = NULL,
  assay = NULL,
  layer = "data",
  method = c("spearman", "pearson", "kendall"),
  smooth.method = "lm",
  show.cor = TRUE,
  show.smooth = TRUE,
  show.rug = FALSE,
  cor.digits = 3,
  cor.size = 4,
  point.size = 1,
  point.alpha = 0.6,
  smooth.size = 1,
  smooth.color = "#fdc086",
  show.se = TRUE,
  ncol = 3,
  palette = "Paired",
  palcolor = NULL,
  point.color = "#984ea3",
  rug.color = "#7fc97f",
  title = NULL,
  marginal = c("none", "density", "histogram", "boxplot", "violin", "densigram"),
  marginal.size = 5,
  raster = NULL,
  raster.dpi = 300,
  ...
)
}
\arguments{
\item{object}{A Seurat object or a data.frame containing the variables
to plot.}

\item{var1}{Character. First variable (x-axis): a gene name, metadata
column, or data.frame column name.}

\item{var2}{Character. Second variable (y-axis): a gene name, metadata
column, or data.frame column name.}

\item{group.by}{Character. Optional column to color points and compute
per-group correlations. Default: \code{NULL} (single color).}

\item{split.by}{Character. Optional column for faceting
(\code{facet_wrap}). Default: \code{NULL}.}

\item{cells}{Character vector. Cell barcodes to include (Seurat only).
Default: \code{NULL} (all cells).}

\item{assay}{Character. Seurat assay to use for gene expression (Seurat
only). Default: \code{DefaultAssay(object)}.}

\item{layer}{Character. Data layer to extract (Seurat only). Default:
\code{"data"} (log-normalized).}

\item{method}{Correlation method for \code{ggpubr::stat_cor}:
\code{"spearman"}, \code{"pearson"}, or \code{"kendall"}.
Default: \code{"spearman"}.}

\item{smooth.method}{Smoothing method for \code{geom_smooth}. Default:
\code{"lm"}. Set to \code{"loess"}, \code{"gam"}, etc. as needed.}

\item{show.cor}{Logical. Show correlation statistics on the plot.
Default: \code{TRUE}.}

\item{show.smooth}{Logical. Show regression / smooth line.
Default: \code{TRUE}.}

\item{show.rug}{Logical. Show rug plots on the axes.
Default: \code{FALSE}.}

\item{cor.digits}{Integer. Number of decimal digits for correlation display.
Default: 3.}

\item{cor.size}{Numeric. Font size for correlation text. Default: 4.}

\item{point.size}{Numeric. Size of scatter points. Default: 1.}

\item{point.alpha}{Numeric. Transparency of scatter points (0--1).
Default: 0.6.}

\item{smooth.size}{Numeric. Width of the regression line. Default: 1.}

\item{smooth.color}{Character. Color of the smooth line when
\code{group.by = NULL}. Default: \code{"#fdc086"}.
Ignored when \code{group.by} is set (line color follows group).}

\item{show.se}{Logical. Show confidence interval around smooth line.
Default: \code{TRUE}.}

\item{ncol}{Integer. Number of columns for faceting when \code{split.by}
is set. Default: 3.}

\item{palette}{Character. Color palette name passed to
\code{\link{palette_colors}}. Default: \code{"Paired"}.}

\item{palcolor}{Character vector. Custom colors overriding \code{palette}.
Default: \code{NULL}.}

\item{point.color}{Character. Fixed point color when \code{group.by = NULL}.
Default: \code{"#984ea3"}.}

\item{rug.color}{Character. Color for rug marks. Default: \code{"#7fc97f"}.}

\item{title}{Character. Plot title. Default: \code{NULL} (auto-generated).}

\item{marginal}{Character. Type of marginal distribution plot to add on
the x- and y-axes: \code{"none"} (default), \code{"density"},
\code{"histogram"}, \code{"boxplot"}, \code{"violin"}, or
\code{"densigram"} (histogram + density overlay). Requires \pkg{patchwork}.
Ignored when \code{split.by} is used.}

\item{marginal.size}{Numeric. Relative size of marginal plots compared
to the main scatter panel. Default: 5 (i.e. the main panel is 5 times
larger than the marginal).}

\item{raster}{Logical. If \code{TRUE}, rasterize the point layer via
\code{ggrastr::rasterise()} to reduce file size for large datasets.
Default: \code{NULL} (auto: \code{TRUE} when > 50 000 cells).}

\item{raster.dpi}{Integer. DPI for rasterized points. Default: 300.}

\item{...}{Additional arguments passed to \code{geom_point}.}
}
\value{
A \code{ggplot} object (or a \code{patchwork} object when
  \code{marginal != "none"}).
}
\description{
Create a scatter plot showing the correlation between two variables (genes
and/or metadata columns) in single-cell data. Supports coloring by groups,
faceting, per-group or global correlation statistics, and multiple
correlation methods.
}
\details{
The first argument \code{object} can be either a \strong{Seurat object} or
a \strong{data.frame} (/ tibble).

\strong{When a Seurat object is provided}, variables \code{var1} and
\code{var2} can be:
\itemize{
  \item Gene names (expression extracted from the specified assay/layer).
  \item Metadata column names in \code{object@meta.data}.
  \item Any combination of the two.
}

\strong{When a data.frame is provided}, \code{var1}, \code{var2},
\code{group.by}, and \code{split.by} must all be column names of the
data.frame.  The Seurat-specific parameters (\code{assay}, \code{layer},
\code{cells}) are ignored.

When \code{group.by} is set, points are colored by the grouping variable
and correlation statistics (\code{ggpubr::stat_cor}) are shown per group.
When \code{split.by} is set, the plot is faceted by that variable.

When \code{marginal} is set to a plot type other than \code{"none"},
marginal distribution plots are added to the x- and y-axes via
\code{patchwork} layout. Supported types include density, histogram,
boxplot, violin, and densigram (histogram + density overlay). When
\code{group.by} is set, the marginal plots are colored/filled by group.
Note: marginal plots are incompatible with faceting (\code{split.by});
if both are specified, marginal plots are silently skipped.

\strong{Note:} When marginal plots are added, the return value is a
\code{patchwork} object. You can still use \code{patchwork::&} or
\code{patchwork::plot_annotation()} for further modifications.
}
\examples{
\dontrun{
library(scMMR)

# --- Seurat object input ---
# Two genes
PlotScatter(seu, var1 = "METTL3", var2 = "SETD2")

# Gene vs metadata
PlotScatter(seu, var1 = "nFeature_RNA", var2 = "PTH",
            method = "pearson")

# Color by cell type
PlotScatter(seu, var1 = "TP53", var2 = "MDM2",
            group.by = "celltype", palette = "npg")

# With marginal boxplot
PlotScatter(seu, var1 = "TP53", var2 = "MDM2",
            group.by = "celltype", marginal = "boxplot")

# --- data.frame input ---
df <- data.frame(x = rnorm(200), y = rnorm(200),
                 grp = sample(c("A", "B"), 200, replace = TRUE))
PlotScatter(df, var1 = "x", var2 = "y")
PlotScatter(df, var1 = "x", var2 = "y", group.by = "grp",
            marginal = "density")
}

}
\seealso{
\code{\link{palette_colors}}
}
