% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/de.R
\name{RunGsea}
\alias{RunGsea}
\title{Run Gene Set Enrichment Analysis on DE Results}
\usage{
RunGsea(
  de_result,
  geneset = list(species = "Homo sapiens", collection = "H"),
  group.by = "group1",
  score.by = "avg_log2FC",
  de_threshold = NULL,
  scoreType = c("std", "pos", "neg"),
  minGSSize = 10,
  maxGSSize = 500,
  pvalueCutoff = 1,
  p.adjust.method = "BH",
  clean.names = TRUE,
  prefix = NULL,
  cores = 1,
  seed = 11,
  verbose = TRUE
)
}
\arguments{
\item{de_result}{A data.frame from \code{\link{RunDE}}, containing at
least \code{gene} and \code{score.by} columns.}

\item{geneset}{Gene set specification. Supports multiple formats:
\describe{
  \item{msigdbr args}{A list with msigdbr parameters, e.g.
    \code{list(species = "Homo sapiens", collection = "H")}.}
  \item{data.frame}{With columns \code{gs_name} and \code{gene_symbol}.}
  \item{named list}{E.g. \code{list(PathA = c("TP53", "BRCA1"))}.}
  \item{GMT file}{Character path to a \code{.gmt} file.}
}}

\item{group.by}{Column in \code{de_result} to split groups. Default:
\code{"group1"}.}

\item{score.by}{Column used as gene ranking score. Default:
\code{"avg_log2FC"}.}

\item{de_threshold}{Optional character string expression to filter
\code{de_result} before GSEA (e.g. \code{"p_val_adj < 0.05"}).
Default: \code{NULL} (use all genes, standard GSEA practice).}

\item{scoreType}{GSEA score type: \code{"std"}, \code{"pos"}, or
\code{"neg"}. Default: \code{"std"}.}

\item{minGSSize}{Minimum gene set size. Default: 10.}

\item{maxGSSize}{Maximum gene set size. Default: 500.}

\item{pvalueCutoff}{P-value cutoff for GSEA results. Default: 1
(no filtering).}

\item{p.adjust.method}{P-value adjustment method. Default: \code{"BH"}.}

\item{clean.names}{Logical; clean pathway names by removing common
prefixes (e.g. HALLMARK_). Default: TRUE.}

\item{prefix}{Optional prefix to remove from pathway names.}

\item{cores}{Integer; number of parallel cores. Default: 1.}

\item{seed}{Random seed. Default: 11.}

\item{verbose}{Logical; print progress. Default: TRUE.}
}
\value{
A list with components:
  \describe{
    \item{\code{enrichment}}{Data.frame of all GSEA results across groups.}
    \item{\code{results}}{Named list of individual \code{gseaResult} objects
      (one per group), usable with \code{enrichplot::gseaplot2()}.}
    \item{\code{input}}{Data.frame of the (filtered) input gene lists.}
  }
}
\description{
Perform GSEA for each group in a \code{\link{RunDE}} result data.frame.
Each group is analyzed independently using \code{clusterProfiler::GSEA()},
and the individual \code{gseaResult} objects are preserved for downstream
visualization (e.g., classic GSEA running score plots via
\code{enrichplot::gseaplot2()}).
}
\examples{
\dontrun{
markers <- RunDE(seu, group.by = "celltype")
res <- RunGsea(markers,
               geneset = list(species = "Homo sapiens", collection = "H"))

# Summary table
head(res$enrichment)

# Classic GSEA running score plot
enrichplot::gseaplot2(res$results[["T_cell"]], geneSetID = 1)

# Custom GMT file
res2 <- RunGsea(markers, geneset = "path/to/custom.gmt")
}

}
\seealso{
\code{\link{RunDE}}, \code{\link{RunGseaEnrich}}
}
