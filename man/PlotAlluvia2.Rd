% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PlotAlluvia2.R
\name{PlotAlluvia2}
\alias{PlotAlluvia2}
\title{Alluvial Plot with Gaps Between Cell Types}
\usage{
PlotAlluvia2(
  cellmeta,
  by = "group",
  fill = "celltype",
  palette = "lancet",
  palcolor = NULL,
  flow.alpha = 0.4,
  bar.width = 0.5,
  bar.color = "gray50",
  gap = 0.01,
  show.label = FALSE,
  label.size = 3,
  label.color = "black",
  label.box = TRUE,
  label.fill = "white",
  label.alpha = 1,
  show.pct = TRUE,
  label.style = c("all", "default", "n", "pct", "name"),
  label.min.pct = 0,
  y.type = c("percent", "count"),
  legend.ncol = 1,
  base.size = 15,
  curve.type = "linear"
)
}
\arguments{
\item{cellmeta}{A Seurat object or data.frame containing cell metadata.}

\item{by}{Character string specifying the grouping variable.
Default \code{"group"}.}

\item{fill}{Character string specifying the cell type variable.
Default \code{"celltype"}.}

\item{palette}{Character. Colour palette name passed to
\code{\link{palette_colors}}. Default \code{"lancet"}.}

\item{palcolor}{Character vector. Custom colours overriding
\code{palette}. Default \code{NULL}.}

\item{flow.alpha}{Numeric (0-1), transparency of alluvial flows.
Default 0.4.}

\item{bar.width}{Numeric (0-1), width of stratum bars. Default 0.5.}

\item{bar.color}{Stratum bar border colour. Default \code{"gray50"}.}

\item{gap}{Numeric. Size of gap between strata as a fraction of total
height. Default 0.005. Set 0 for no gaps (same as PlotAlluvia).}

\item{show.label}{Logical. Default \code{FALSE}.}

\item{label.size}{Numeric. Default 3.}

\item{label.color}{Character. Default \code{"black"}.}

\item{label.box}{Logical. If \code{TRUE}, boxed labels. Default \code{FALSE}.}

\item{label.fill}{Background fill for boxed labels. Default \code{"white"}.}

\item{label.alpha}{Transparency of boxed label background. Default \code{1}.}

\item{show.pct}{Logical. Default \code{FALSE}.}

\item{label.style}{Character. Label format: \code{"default"}, \code{"all"},
\code{"n"}, \code{"pct"}, or \code{"name"}.}

\item{label.min.pct}{Numeric (0-1). Minimum proportion to show label.
Default 0.}

\item{y.type}{Character. \code{"percent"} (default) or \code{"count"}.}

\item{legend.ncol}{Integer. Default 1.}

\item{base.size}{Numeric. Default 15.}

\item{curve.type}{Character. Flow curve type. Default \code{"sigmoid"}.
Options: \code{"linear"}, \code{"cubic"}, \code{"quintic"},
\code{"sine"}, \code{"arctangent"}, \code{"sigmoid"},
\code{"xspline"}.}
}
\value{
A ggplot object.
}
\description{
Create an alluvial plot showing cell type composition changes across
conditions/groups, with visible gaps between strata (cell types).
Based on \pkg{ggalluvial}, identical to \code{\link{PlotAlluvia}} but
with spacing between cell type strata for cleaner visual separation.
Gaps are implemented by inserting invisible (transparent) strata between
real cell types.
}
\examples{
\dontrun{
library(ToyData)

# --- Basic usage (with default gaps) ---
PlotAlluvia2(toy_test)

# --- Adjust gap size ---
PlotAlluvia2(toy_test, gap = 0.01)   # larger gaps
PlotAlluvia2(toy_test, gap = 0)      # no gaps (same as PlotAlluvia)

# --- Labels ---
PlotAlluvia2(toy_test, label.style = "pct", label.min.pct = 0.05)
PlotAlluvia2(toy_test, label.style = "pct", label.min.pct = 0.05,
             label.box = TRUE)

# --- Custom colours ---
PlotAlluvia2(toy_test, palcolor = UtilsR::pal_paraSC)
PlotAlluvia2(toy_test, palette = "Paired")

# --- Raw counts ---
PlotAlluvia2(toy_test, y.type = "count", label.style = "n")
}

}
