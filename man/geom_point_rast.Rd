% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster.R
\name{geom_point_rast}
\alias{geom_point_rast}
\title{Rasterised point geom}
\usage{
geom_point_rast(..., raster.dpi = 300, raster.dev = NULL, raster.scale = 1)
}
\arguments{
\item{...}{Additional arguments passed to \code{\link[ggplot2]{geom_point}}.}

\item{raster.dpi}{Numeric. Raster resolution. Default \code{300}.}

\item{raster.dev}{Character. Backend device. Default \code{NULL}
(auto-detect).}

\item{raster.scale}{Numeric. Scale factor. Default \code{1}.}
}
\value{
A rasterised ggplot2 layer.
}
\description{
Convenience wrapper that creates a \code{geom_point} layer and rasterises
it via \code{\link{rasterise_layer}}.
}
\examples{
\dontrun{
library(ggplot2)
df <- data.frame(x = rnorm(1e5), y = rnorm(1e5))
ggplot(df, aes(x, y)) + geom_point_rast(alpha = 0.3)
}

}
