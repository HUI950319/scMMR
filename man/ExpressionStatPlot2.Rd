% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ExpressionStatPlot2.R
\name{ExpressionStatPlot2}
\alias{ExpressionStatPlot2}
\title{Expression distribution plots (violin / box / bar / dot)}
\usage{
ExpressionStatPlot2(
  seu,
  features,
  group.by,
  split.by = NULL,
  assay = NULL,
  layer = "data",
  cells = NULL,
  plot.by = c("group", "feature"),
  type = c("violin", "box", "bar", "dot"),
  fill.by = c("group", "feature"),
  palette = NULL,
  alpha = 0.8,
  add_box = FALSE,
  box_color = "black",
  box_width = 0.1,
  add_point = FALSE,
  pt.color = "grey30",
  pt.size = NULL,
  pt.alpha = 1,
  jitter.width = 0.4,
  jitter.height = 0.1,
  add_trend = FALSE,
  trend_color = "black",
  trend_linewidth = 1,
  trend_ptsize = 2,
  comparisons = NULL,
  ref_group = NULL,
  pairwise_method = "wilcox.test",
  multiplegroup_comparisons = FALSE,
  multiple_method = "kruskal.test",
  sig_label = c("p.signif", "p.format"),
  sig_labelsize = 3.5,
  bg.by = NULL,
  bg_palette = NULL,
  bg_alpha = 0.15,
  same.y.lims = FALSE,
  y.min = NULL,
  y.max = NULL,
  y.nbreaks = 5,
  sort = FALSE,
  stack = FALSE,
  flip = FALSE,
  title = NULL,
  subtitle = NULL,
  xlab = NULL,
  ylab = NULL,
  legend.position = "right",
  legend.direction = "vertical",
  aspect.ratio = NULL,
  base_size = 14,
  facet_nrow = NULL,
  facet_ncol = NULL,
  combine = TRUE,
  force = FALSE,
  seed = 11
)
}
\arguments{
\item{seu}{A Seurat object **or** a data.frame containing feature columns
and the grouping column.}

\item{features}{Character vector of feature names (gene names or metadata /
data.frame column names).}

\item{group.by}{Column name used for grouping (x-axis in \code{"group"}
mode, fill colour in \code{"feature"} mode).}

\item{split.by}{Column name for splitting into separate sub-plots.  Passed
through to \code{plt_con()} in \code{"group"} mode; creates a patchwork
row in \code{"feature"} mode.  Default \code{NULL}.}

\item{assay}{Seurat assay to use.  \code{NULL} = active assay.}

\item{layer}{Seurat layer to pull expression from.  Default \code{"data"}.}

\item{cells}{Optional character vector of cell barcodes to subset.
\code{NULL} = all cells.}

\item{plot.by}{Axis orientation.  \code{"group"} (default) or
\code{"feature"}.}

\item{type}{Plot geometry: \code{"violin"} (default), \code{"box"},
\code{"bar"}, or \code{"dot"}.}

\item{fill.by}{What drives fill colour: \code{"group"} (default) or
\code{"feature"}.}

\item{palette}{Colour palette name (passed to \code{UtilsR::plt_con()}).
\code{NULL} = package default.}

\item{alpha}{Fill transparency (0–1).  Default \code{0.8}.}

\item{add_box}{Logical.  Overlay a narrow box-plot (median + IQR) on violin
or bar.  Default \code{FALSE}.}

\item{box_color}{Colour of the overlay box-plot.  Default \code{"black"}.}

\item{box_width}{Width of the overlay box-plot.  Default \code{0.1}.}

\item{add_point}{Logical.  Overlay jittered raw points.  Default
\code{FALSE}.}

\item{pt.color}{Point colour.  Default \code{"grey30"}.}

\item{pt.size}{Point size.  \code{NULL} = auto (scaled by cell count).}

\item{pt.alpha}{Point transparency.  Default \code{1}.}

\item{jitter.width}{Horizontal jitter extent.  Default \code{0.4}.}

\item{jitter.height}{Vertical jitter extent.  Default \code{0.1}.}

\item{add_trend}{Logical.  Connect group medians / means with a trend line.
Default \code{FALSE}.}

\item{trend_color}{Trend line colour.  Default \code{"black"}.}

\item{trend_linewidth}{Trend line width.  Default \code{1}.}

\item{trend_ptsize}{Trend point size.  Default \code{2}.}

\item{comparisons}{List of length-2 vectors for pairwise statistical tests.
\code{NULL} = none.}

\item{ref_group}{Reference group for one-vs-all comparisons.  \code{NULL} =
no reference.}

\item{pairwise_method}{Statistical test for pairwise comparisons.  Default
\code{"wilcox.test"}.}

\item{multiplegroup_comparisons}{Logical.  Add an overall group comparison
label.  Default \code{FALSE}.}

\item{multiple_method}{Test for overall comparison.  Default
\code{"kruskal.test"}.}

\item{sig_label}{Significance label format: \code{"p.signif"} (default) or
\code{"p.format"}.}

\item{sig_labelsize}{Text size for significance labels.  Default \code{3.5}.}

\item{bg.by}{Column name for background colour bands (must be a
super-grouping of \code{group.by}).  \code{NULL} = none.}

\item{bg_palette}{Palette for background bands.  \code{NULL} = default.}

\item{bg_alpha}{Transparency of background bands.  Default \code{0.15}.}

\item{same.y.lims}{Logical.  Share y limits across all panels.  Default
\code{FALSE}.}

\item{y.min}{Minimum y value (numeric) or quantile string (e.g.
\code{"q1"}).  \code{NULL} = data minimum.}

\item{y.max}{Maximum y value or quantile string.  \code{NULL} = data
maximum.}

\item{y.nbreaks}{Number of y-axis tick breaks.  Default \code{5}.}

\item{sort}{Logical or \code{"increasing"}.  Sort groups by median of the
first feature.  Default \code{FALSE}.}

\item{stack}{Logical.  Stack all features into a single faceted plot
(\code{"group"} mode only).  Default \code{FALSE}.}

\item{flip}{Logical.  Flip coordinates (horizontal layout).  Default
\code{FALSE}.}

\item{title}{Plot title.  \code{NULL} = none.}

\item{subtitle}{Plot subtitle.  \code{NULL} = none.}

\item{xlab}{X-axis label.  \code{NULL} = auto.}

\item{ylab}{Y-axis label.  \code{NULL} = auto.}

\item{legend.position}{Legend position.  Default \code{"right"}.}

\item{legend.direction}{Legend direction.  Default \code{"vertical"}.}

\item{aspect.ratio}{Aspect ratio of each panel.  \code{NULL} = free.}

\item{base_size}{Base font size.  Default \code{14}.}

\item{facet_nrow, facet_ncol}{Number of rows / columns in the combined
patchwork layout.}

\item{combine}{Logical.  Combine panels with patchwork.  Default
\code{TRUE}.}

\item{force}{Logical.  Skip the high-cardinality group check.  Default
\code{FALSE}.}

\item{seed}{Random seed for jitter reproducibility.  Default \code{11}.}
}
\value{
A ggplot2 or patchwork object.
}
\description{
Visualises the expression of one or more features stratified by a grouping
variable.  Accepts a Seurat object (genes are fetched from the specified
assay / layer) or a plain data.frame.
}
\details{
Two axis orientations are available via \code{plot.by}:
\itemize{
  \item \code{"group"} (default) — one panel per feature; groups on the
    x-axis.  Powered by \code{UtilsR::plt_con()}.
  \item \code{"feature"} — one combined panel; features on the x-axis,
    groups encoded by fill colour.
}
}
\examples{
\dontrun{
# Seurat object — expression of two genes by cluster
ExpressionStatPlot2(seu, features = c("CD3D", "CD8A"),
                    group.by = "seurat_clusters")

# Feature-axis mode: features on x, cell types as colour
ExpressionStatPlot2(seu, features = c("CD3D", "CD8A", "MS4A1"),
                    group.by = "cell_type", plot.by = "feature",
                    type = "violin")

# Plain data.frame
df <- data.frame(GeneA = rnorm(100), GeneB = rnorm(100),
                 cluster = sample(letters[1:4], 100, replace = TRUE))
ExpressionStatPlot2(df, features = c("GeneA", "GeneB"),
                    group.by = "cluster", type = "box")
}

}
