% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/de.R
\name{RunDE}
\alias{RunDE}
\title{Run Differential Expression Test}
\usage{
RunDE(
  seu,
  group.by = NULL,
  split.by = NULL,
  group1 = NULL,
  group2 = NULL,
  cells1 = NULL,
  cells2 = NULL,
  features = NULL,
  markers_type = c("all", "paired", "conserved", "disturbed"),
  grouping.var = NULL,
  meta.method = c("maximump", "minimump", "wilkinsonp", "meanp", "sump", "votep"),
  test.use = "wilcox",
  only.pos = TRUE,
  fc.threshold = 1.5,
  p_val_cutoff = NULL,
  logfc_cutoff = NULL,
  base = 2,
  pseudocount.use = 1,
  mean.fxn = NULL,
  min.pct = 0.1,
  min.diff.pct = -Inf,
  max.cells.per.ident = Inf,
  latent.vars = NULL,
  min.cells.feature = 3,
  min.cells.group = 3,
  norm.method = "LogNormalize",
  p.adjust.method = "bonferroni",
  downsample = NULL,
  layer = "data",
  assay = NULL,
  cores = max(1L, parallel::detectCores()\%/\%2),
  seed = 11,
  verbose = TRUE
)
}
\arguments{
\item{seu}{A Seurat object.}

\item{group.by}{Column name in \code{meta.data} for grouping. If
\code{NULL}, uses \code{Idents(seu)}.}

\item{split.by}{Optional column name for a second grouping variable
(e.g. condition). When provided, cells are grouped by
\code{group.by x split.by} composite identity for DE testing.
Result columns \code{celltype} and \code{condition} are added.
Default: NULL.}

\item{group1}{Character vector of group labels for the first group.}

\item{group2}{Character vector of group labels for the second group.}

\item{cells1}{Character vector of cell barcodes for the first group.
Overrides \code{group1}.}

\item{cells2}{Character vector of cell barcodes for the second group.}

\item{features}{Character vector of genes to test. Default: all genes.}

\item{markers_type}{Type of marker analysis. One of \code{"all"}
(one-vs-rest), \code{"paired"} (all pairwise), \code{"conserved"}
(across conditions), or \code{"disturbed"} (condition-specific).}

\item{grouping.var}{Column for condition variable (required for
conserved/disturbed).}

\item{meta.method}{Method for combining p-values in conserved markers.
One of \code{"maximump"}, \code{"minimump"}, \code{"wilkinsonp"},
\code{"meanp"}, \code{"sump"}, \code{"votep"}.}

\item{test.use}{DE test method. Passed to \code{Seurat::FindMarkers}.}

\item{only.pos}{Logical; return only positive markers. Default: TRUE.}

\item{fc.threshold}{Fold change threshold (linear scale, >= 1).
Pre-filter before testing: genes below this threshold are skipped.
Default: 1.5.}

\item{p_val_cutoff}{Numeric or NULL. Post-filter: keep only results with
\code{p_val_adj < p_val_cutoff}. Default: NULL (no filtering).}

\item{logfc_cutoff}{Numeric or NULL. Post-filter: keep only results with
\code{|avg_log2FC| > logfc_cutoff}. Default: NULL (no filtering).}

\item{base}{Log base for fold change. Default: 2.}

\item{pseudocount.use}{Pseudocount for log fold change. Default: 1.}

\item{mean.fxn}{Custom mean function for fold change.}

\item{min.pct}{Minimum fraction of cells expressing the gene. Default: 0.1.}

\item{min.diff.pct}{Minimum difference in fraction between groups.}

\item{max.cells.per.ident}{Maximum cells per identity for testing.}

\item{latent.vars}{Latent variables for certain tests (MAST, LR, etc.).}

\item{min.cells.feature}{Minimum cells expressing feature. Default: 3.}

\item{min.cells.group}{Minimum cells per group. Default: 3.}

\item{norm.method}{Normalization method. Default: "LogNormalize".}

\item{p.adjust.method}{P-value adjustment method. Default: "bonferroni".}

\item{downsample}{Integer or NULL. If specified, downsample each identity
class to at most this many cells before DE testing. Uses
\code{subset(seu, downsample = downsample)}. Default: NULL.}

\item{layer}{Data layer to use. Default: "data".}

\item{assay}{Assay to use. Default: \code{DefaultAssay(seu)}.}

\item{cores}{Integer. Number of parallel cores. Default:
\code{parallel::detectCores() \%/\% 2} (half of available cores).
Set to 1 to disable parallelism.}

\item{seed}{Random seed. Default: 11.}

\item{verbose}{Logical; print progress messages. Default: TRUE.}
}
\value{
A data.frame of DE results with columns: p_val, avg_log2FC,
  pct.1, pct.2, p_val_adj, gene, group1, group2, test_group_number,
  test_group.
}
\description{
Perform differential expression (DE) testing across cell groups in a
Seurat object. Supports four marker types: one-vs-rest (\code{"all"}),
pairwise (\code{"paired"}), conserved across conditions
(\code{"conserved"}), and condition-specific (\code{"disturbed"}).
Optionally downsamples cells before testing for balanced comparisons.
}
\examples{
\dontrun{
# One-vs-rest DE
markers <- RunDE(seu, group.by = "celltype")

# Pairwise with downsampling
markers <- RunDE(seu, group.by = "celltype",
                 markers_type = "paired", downsample = 200)

# Conserved markers across conditions
markers <- RunDE(seu, group.by = "celltype",
                 grouping.var = "group",
                 markers_type = "conserved")

# Joint celltype x condition DE (for downstream GSEA)
markers <- RunDE(seu, group.by = "celltype", split.by = "condition")

# With post-filtering: padj < 0.05 and |log2FC| > 1
markers <- RunDE(seu, group.by = "celltype",
                 p_val_cutoff = 0.05, logfc_cutoff = 1)
}

}
\seealso{
\code{\link[Seurat]{FindMarkers}},
  \code{\link{RunPathwayAnalysis}}
}
